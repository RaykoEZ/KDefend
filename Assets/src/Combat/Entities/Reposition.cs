using UnityEngine;

public class Reposition : Formation
{
    public override bool TryGetFormationPosition(BaseEntity chaseTarget, out Vector3 warpPosition)
    {
        throw new System.NotImplementedException();
    }
}
